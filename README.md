# memetrics

Mongo-backed metrics mate for Metabase measures and metadata monitoring.

Pronounced muh-MEH-tricks.

## dev setup

```bash
pip install -e .
cp example.env .env
# env vars
python -m memetrics
```
